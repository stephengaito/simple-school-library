
  <label><=label=></label>
  <input type="number" id="<=name=>" name="<=name=>" step="1"
  {%  if <=firstValue=> is defined %}
    value="{{<=firstValue=>}}"
  <% for anAlternate in alternateValues %>
  {% elif <=anAlternate=> is defined %}
    value="{{<=anAlternate=>}}"
  <% endfor %>
  {% else %}
    value="<=defaultValue=>"
  {% endif %}
  />
